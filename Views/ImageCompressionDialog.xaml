<mah:MetroWindow
    x:Class="ImageFolderManager.Views.ImageCompressionDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    Title="Image Compression"
    Width="460"
    Height="400"
    WindowStartupLocation="CenterOwner"
    ResizeMode="NoResize"
    ShowMinButton="False"
    ShowMaxRestoreButton="False"
    TitleCharacterCasing="Normal"
    Background="{DynamicResource App.Brush.Window}">

    <Grid Margin="20,14,20,14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Folder name -->
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="&#xE8B7;" FontFamily="Segoe MDL2 Assets" FontSize="14" VerticalAlignment="Center" Margin="0,0,7,0" Foreground="{StaticResource StatusInfoBrush}"/>
            <TextBlock x:Name="FolderNameText"
                       Text="Selected Folder"
                       Foreground="{DynamicResource App.Brush.TextPrimary}"
                       FontSize="13" FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       TextTrimming="CharacterEllipsis"
                       MaxWidth="370"/>
        </StackPanel>

        <!-- Quality slider -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="52"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Compression Quality"
                           Foreground="{DynamicResource App.Brush.TextPrimary}"
                           FontSize="12" Margin="0,0,0,5"/>
                <Slider x:Name="QualitySlider"
                        Minimum="1" Maximum="100" Value="80"
                        TickFrequency="5" IsSnapToTickEnabled="True"
                        SmallChange="1" LargeChange="10"
                        ValueChanged="QualitySlider_ValueChanged"/>
                <TextBlock Text="1 = smallest file,   100 = best quality"
                           Foreground="{DynamicResource App.Brush.TextMuted}"
                           FontSize="10" Margin="0,3,0,0"/>
            </StackPanel>
            <Border Grid.Column="1"
                    Background="{DynamicResource MahApps.Brushes.Accent}"
                    CornerRadius="4" Margin="8,0,0,14"
                    VerticalAlignment="Center" Padding="4,2">
                <TextBlock x:Name="QualityValueText" Text="80"
                           Foreground="{DynamicResource App.Brush.TextPrimary}" FontSize="15" FontWeight="Bold"
                           HorizontalAlignment="Center"/>
            </Border>
        </Grid>

        <!-- Size preview panel -->
        <Border Grid.Row="4"
                Background="{DynamicResource App.Brush.Surface1}"
                BorderBrush="{DynamicResource App.Brush.Border}"
                BorderThickness="1" CornerRadius="4" Padding="12,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="6"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="6"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Estimated Size Change"
                           Foreground="{DynamicResource App.Brush.TextPrimary}"
                           FontSize="12" FontWeight="SemiBold"/>

                <!-- Before / After -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                        <TextBlock Text="Before"
                                   Foreground="{DynamicResource App.Brush.TextMuted}"
                                   FontSize="11" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="BeforeSizeText" Text="—"
                                   Foreground="{DynamicResource App.Brush.TextPrimary}"
                                   FontSize="16" FontWeight="SemiBold"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Grid.Column="1" Text="&#x2192;"
                               Foreground="{DynamicResource App.Brush.TextMuted}"
                               FontSize="18" VerticalAlignment="Center"
                               Margin="10,4,10,0"/>
                    <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                        <TextBlock Text="After (estimated)"
                                   Foreground="{DynamicResource App.Brush.TextMuted}"
                                   FontSize="11" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="AfterSizeText" Text="—"
                                   Foreground="{StaticResource StatusInfoBrush}"
                                   FontSize="16" FontWeight="SemiBold"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <!-- Calculating spinner -->
                <ProgressBar x:Name="EstimateProgress"
                             Grid.Row="4" Height="3"
                             IsIndeterminate="True" Visibility="Collapsed"/>

                <!-- Saved label -->
                <TextBlock x:Name="SavedText" Grid.Row="4"
                           Text="" Foreground="{StaticResource StatusSuccessBrush}"
                           FontSize="12" FontWeight="SemiBold"
                           HorizontalAlignment="Center" Visibility="Collapsed"/>

                <!-- Note -->
                <TextBlock x:Name="EstimateNote" Grid.Row="6"
                           Text="Calculating estimate..."
                           Foreground="{DynamicResource App.Brush.TextMuted}"
                           FontSize="10" HorizontalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Delete source checkbox -->
        <CheckBox x:Name="DeleteSourceCheckBox"
                  Grid.Row="6"
                  Content="Delete source files after compression"
                  Foreground="{DynamicResource App.Brush.TextPrimary}"
                  FontSize="13" IsChecked="False"
                  Checked="DeleteSourceCheckBox_CheckedChanged"
                  Unchecked="DeleteSourceCheckBox_CheckedChanged"/>

        <!-- Delete warning -->
        <TextBlock x:Name="DeleteWarningText"
                   Grid.Row="7"
                   Text="Original images will be permanently deleted. This cannot be undone."
                   Foreground="{StaticResource StatusWarningBrush}"
                   FontSize="11" TextWrapping="Wrap"
                   Margin="0,4,0,0" Visibility="Collapsed"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="9" Style="{StaticResource DialogFooterPanel}">
            <Button x:Name="CancelButton"
                    Content="Cancel"
                    Style="{StaticResource SecondaryButton}"
                    Width="80" IsCancel="True"
                    Margin="{StaticResource DialogButtonSpacing}"
                    Click="CancelButton_Click"/>
            <Button x:Name="CompressButton"
                    Content="Compress"
                    Style="{StaticResource PrimaryButton}"
                    Width="90" IsDefault="True"
                    Click="CompressButton_Click"/>
        </StackPanel>
    </Grid>
</mah:MetroWindow>
